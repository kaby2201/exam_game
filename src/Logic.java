/*public class Logic {
    HeroBlock heroBlock;
    SmashBlock smashBlock;

    int random  = (int)(Math.random() * 2);

    public Logic(GUI gui) {
        heroBlock = new HeroBlock();
        smashBlock = new SmashBlock();
    }


    public void Random() {
        switch (random) {
            case 0:
                smashBlock.drawMe(gui.getGraphics);
        }
    }
}*/
